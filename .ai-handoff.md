# AI Handoff Context
*Last updated: 2026-06-22 | From: Claude Code | Project: Spotify Notion Widget*

## Working Directory
`C:\Users\pragy\Documents\Coding\Spotify Notion Widget`

## Task in Progress
UI polish pass on the Spotify Notion Widget — Red Fuji gradient redesign, Spotify Radio queue, album art square fix, prev/next skip buttons, and compact single-panel mode for narrow Notion embeds. All features implemented; pending visual verification.

## Files Modified / Created
- `app/globals.css` — Red Fuji gradient theme; album art as 1:1 square capped at 200px (`width: min(100%, 200px); aspect-ratio: 1/1`); `.close-btn`, `.player-controls`, `.control-btn`, `.spotify-embed` styles; `@media (max-width: 650px)` compact mode (single panel, album fills space, embed collapses to 80px bar)
- `app/page.tsx` — Added `CompactView` type + `compactView` state; `skip()` function calling `/api/skip`; X close button in player panel; `album-wrapper` div; prev/next control buttons; `setCompactView("player")` on track select
- `lib/spotify.ts` — Added `skipSpotifyTrack(accessToken, direction)` export; added `getSpotifyRecommendations()` for Spotify Radio queue
- `app/api/skip/route.ts` — New route: POST handler calling `skipSpotifyTrack` → `POST /me/player/next|previous`
- `app/api/play/route.ts` — Updated to start playback then auto-queue 10 recommendations via `getSpotifyRecommendations`

## Key Decisions Made
- Album art breakpoint raised to `650px` (was 480px) — Notion inline embeds are typically ~510–640px wide
- Album art capped at `min(100%, 200px)` on wide screens — full-panel width (400px) was too large
- Skip buttons call native Spotify API, not the iframe embed — by design (Spotify architecture limitation); only works with Premium + active native Spotify device
- Compact mode uses `data-view` attribute + CSS to hide/show panels — no JS layout complexity
- Spotify Radio uses `/v1/recommendations?seed_tracks=<id>` — 10 tracks auto-queued after playback starts

## Completed This Session
- Red Fuji gradient theme (blue sky → amber mountain)
- Spotify Radio: auto-queues 10 recommendations after selecting a track
- Album art: always square, `aspect-ratio: 1/1`, capped at 200px on wide screens
- Compact mode: single-panel with X button, album fills screen, embed bar at bottom (80px)
- Prev/Next skip buttons in player panel
- `app/api/skip/route.ts` created
- TypeScript clean (`npx tsc --noEmit` — no errors)

## Next Steps (start here)
1. Run `npm run dev` and open `http://127.0.0.1:3000`
2. On wide viewport (>650px): verify album art is ~200×200px square, prev/next buttons visible below album
3. On narrow viewport (<650px or resize browser): verify compact mode triggers — track selection auto-switches to player view, album fills space above the 80px Spotify bar, X button top-right returns to search
4. Test skip buttons: requires native Spotify Premium open on a device — they control the native app player, not the iframe
5. Decide on branch: push as PR, merge locally, or keep (`main` branch, no PR yet)

## Open Questions / Blockers
- Skip buttons only control the native Spotify app (not the iframe embed) — user was confused by this; may want a tooltip or UI note explaining the limitation
- Branch is on `main` — no PR created yet; user hasn't chosen to push/merge/keep/discard

## Extra Context
- Spotify iframe embed is sandboxed — `POST /me/player/next` has no effect on it; it only controls whichever Spotify app is active on the user's devices
- Compact mode album uses `flex: 1; min-height: 0` on `.selected-track` and `.album-wrapper` to fill available space between close button and controls/embed bar
- `getSpotifyUserAccessToken` lives in `lib/spotify-session.ts` (pre-existing, handles httpOnly cookie token storage + refresh)
- Run locally at `http://127.0.0.1:3000` (not `localhost`) — Spotify OAuth redirect URI is configured for `127.0.0.1`
